{
  "name": "backend-phonebook",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \\\"Error: no test specified\\\" && exit 1",
    "dev": "nodemon index.js",
	"build:ui": "rm -rf build && cd frontend/ && npm run build && cp -r build ../build",
    "deploy": "git add . && git commit -m uibuild && git push && npm run deploy"
  },
  "author": "Mert Barut",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
